import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-first-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
